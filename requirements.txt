# Python 3.13 (verified with 3.13.14 on Windows 11). These exact versions reproduce every table and figure.
#   python -m venv .venv
#   .venv\Scripts\activate          (macOS/Linux: source .venv/bin/activate)
#   pip install -r requirements.txt

# analysis (notebooks/, pipeline/, scripts/figures/)
numpy==2.5.0
pandas==3.0.3          # the notebooks were written for pandas 2; the few affected idioms were updated
scipy==1.18.0
matplotlib==3.11.0
seaborn==0.13.2
statsmodels==0.15.0
biopython==1.88
tqdm==4.70.1
requests==2.34.2
pyarrow==24.0.0        # parquet I/O
openpyxl==3.1.5        # .xlsx input (Lambert et al. 2018 Table S7)

# running the notebooks headless (run.py)
nbclient==0.11.0
nbformat==5.10.4
ipykernel==7.3.0
ipython==9.15.0

# figure previews for FIGURES.md (scripts/build_figure_catalog.py)
pymupdf==1.28.0

# Cluster only, not needed here:
#   goatools     notebooks/go_enrichment_CLUSTER_ONLY.ipynb (GO annotation files live on the AKEY cluster)
#   fastparquet  the cluster branch of notebooks 01/02 that reads ensg_to_upkb.parquet
